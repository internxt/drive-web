import { useEffect, useState } from 'react';

const FileVideoViewer = ({ file }) => {
  return (
    <div>
      <video id="video-Inxt" controls></video>
    </div>
  );
};

export default FileVideoViewer;
